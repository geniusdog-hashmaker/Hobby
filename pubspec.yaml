name: hobby
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  # firebase_core: "^1.7.0"
  # cloud_firestore: "^2.5.3"


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0

flutter:
  assets:
      - assets/images/kids.png
      - assets/images/ag.png
      - assets/images/muzic.png
      - assets/images/thackvando.png
      - assets/images/yunost.png
  # fonts:
  #     - family:
  #         - asset: fonts/Raleway-Regular.ttf
  #         - asset: fonts.Raleway-Bold.ttf
  uses-material-design: true
